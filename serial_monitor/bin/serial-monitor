#!/usr/bin/env ruby

$LOAD_PATH.unshift(File.expand_path('../../lib', __FILE__))
require 'serial_monitor'

sm = SerialMonitor.new(ARGV[0], 38400)

trap("SIGINT") do
  sm.close
  print "\n"
  exit
end

loop { puts sm.gets }
