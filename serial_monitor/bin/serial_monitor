#!/usr/bin/env ruby

$LOAD_PATH.unshift(File.expand_path('../../lib', __FILE__))
require 'serial_monitor'

SerialMonitor.open(ARGV[0], 38400, "\n", "\n") do |sm|
  sleep(2)
  loop { puts sm.gets }
end
